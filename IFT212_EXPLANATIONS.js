// IFT212 - Computer Architecture & Organisation Explanations Database

const IFT212_EXPLANATIONS = {
    "ift212_001": "Architecture (ISA) defines what the programmer sees: instruction set, registers, memory model, and data types. Organization (micro-architecture) defines how it is implemented internally: ALU design, pipeline stages, cache hierarchy — details invisible to the programmer.",
    "ift212_002": "Autoincrement: EA = [Reg], then Reg = Reg + operand_size. The current register value is used as the address first, then the register is bumped to point to the next element. This is ideal for sequential array traversal.",
    "ift212_003": "Autodecrement: Reg = Reg - operand_size first, then EA = [Reg]. The decrement happens BEFORE the access, unlike autoincrement where increment happens AFTER. Notation: LOAD -(Rauto), Ri. Together, these two modes implement stack push/pop operations.",
    "ift212_004": "Each assembly mnemonic (e.g., MOV, ADD, JMP) maps to exactly one binary machine instruction. This 1:1 correspondence makes assembly a symbolic representation of machine code, unlike high-level languages where one statement may compile to many instructions.",
    "ift212_005": "The assembler is a system program that reads assembly source (.asm file) and outputs object code (.obj or .o file) containing binary machine instructions. It also resolves symbolic labels to memory addresses.",
    "ift212_006": "Assembly code directly references CPU-specific registers (eax, ebx on x86; r0-r15 on ARM), instruction encodings, and calling conventions. Code written for x86 will not execute on an ARM processor — it must be completely rewritten for the target architecture.",
    "ift212_007": "An assembly program has three main sections: .data (initialized variables and constants), .bss (uninitialized variables), and .text (executable instructions). The .data section is loaded into a read/write data segment at runtime.",
    "ift212_008": "db = Define Byte. It reserves one byte per character and stores the specified values sequentially in memory. msg db 'Hello', 0 allocates 6 bytes: 5 for the characters H,e,l,l,o and 1 for the null terminator (0).",
    "ift212_009": "$ is the current location counter — it holds the address of the current position in the binary. Since msg is a label at the start of the string, $ - msg gives the number of bytes between the start and current position, which equals the string length.",
    "ift212_010": "int 0x80 is the software interrupt instruction that triggers Linux system call handling. It saves user-mode state, switches to kernel mode, reads the system call number from eax, executes the requested service, then returns control to user code.",
    "ift212_011": "Linux x86 system call numbers: eax=1 → sys_exit, eax=2 → sys_fork, eax=3 → sys_read, eax=4 → sys_write, eax=5 → sys_open. For sys_write: ebx=file descriptor (1=stdout), ecx=buffer address, edx=byte count.",
    "ift212_012": "CPUs have no native if/while/for instructions. Compilers translate these into: CMP (compare), followed by Bxx (conditional branch — BEQ, BNE, BLT, BGT) and JMP (unconditional jump). Every high-level control flow ultimately becomes these primitives.",
    "ift212_013": "If-Then-Else pattern: evaluate condition → if false, branch to ELSE block → execute THEN block → JMP past ELSE block → ELSE block. Without the JMP after the THEN block, execution would fall through sequentially into the ELSE code.",
    "ift212_014": "While loop pattern: LOOP_START: evaluate condition → if false, JMP to LOOP_END → execute loop body → JMP back to LOOP_START → LOOP_END. The backward jump is what creates the repetition — it sends the PC back to re-evaluate the condition.",
    "ift212_015": "CALL instruction: pushes PC+1 (next instruction address) onto stack, then jumps to the procedure address. RET instruction: pops the saved return address from the stack and loads it into the PC, resuming execution after the original CALL site.",
    "ift212_016": "A stack frame (activation record) contains: return address, saved registers, passed parameters, and local variables. Each procedure call creates a new frame; when the procedure returns, its frame is deallocated, restoring the caller's environment.",
    "ift212_017": "Prologue sequence: (1) PUSH FP — save caller's frame pointer, (2) FP = SP — anchor new frame base, (3) SP = SP - N — allocate N bytes for local variables. Epilogue reverses this: SP = FP, POP FP, RET.",
    "ift212_018": "Each recursive call creates a NEW stack frame with its own copies of local variables and its own return address. Without separate frames, each nested call would overwrite the previous call's variables. The stack unwinds correctly as each recursive call completes and its frame is popped.",
    "ift212_019": "After each ALU operation, the CPU updates condition code flags: Z (Zero) — result is 0, N/S (Negative/Sign) — result is negative, C (Carry) — unsigned overflow, V/O (Overflow) — signed overflow. Conditional branch instructions test these flags to decide whether to branch.",
    "ift212_020": "XOR ebx, ebx: produces a 2-byte instruction encoding, executes in 1 clock cycle, and implicitly sets the Zero flag. MOV ebx, 0: produces a 5-byte instruction (opcode + 4-byte immediate). The XOR idiom is a well-known x86 optimization used by compilers and hand-tuned code alike.",
    "ift212_021": "Control signals are electrical lines driven by the Control Unit to coordinate all CPU hardware. Examples: ALUop lines tell the ALU which operation to perform, RegWrite enables writing to a register, MemRead/MemWrite enable memory access, and Branch controls the PC multiplexer.",
    "ift212_022": "Point-to-point: one dedicated wire per control signal — simple but requires many wires for complex CPUs. Decoded: n bits are transmitted and decoded into 2^n signals at the destination — reduces wiring at the cost of a small decoder circuit delay.",
    "ift212_023": "The CU is a finite state machine driven by three inputs: (1) IR opcode — determines which instruction is executing, (2) Condition flags (Z, N, C, V) — determine branch outcomes, (3) Clock — advances the state machine one step per cycle to sequence multi-cycle operations.",
    "ift212_024": "Hardwired CU advantages: fastest possible execution (pure combinational logic, no ROM lookup), minimal latency. Disadvantages: rigid — changing instruction behavior requires physical chip redesign. Used in RISC architectures (ARM, MIPS, RISC-V).",
    "ift212_025": "Microprogrammed CU: each machine instruction opcode indexes into a control store ROM, retrieving a micro-routine of control words. Each microinstruction sets all control signals for one clock cycle. Advantage: flexible (ROM can be patched). Used in CISC architectures (x86).",
    "ift212_026": "Modern x86 (Intel/AMD): a hardware decoder translates x86 instructions into simpler micro-ops (RISC-like). Common micro-ops run through hardwired fast paths. Rare/complex instructions fall back to a microcode ROM. This hybrid balances CISC compatibility with RISC-level performance.",
    "ift212_027": "CPU clock: ~3-4 GHz = ~0.25-0.33 ns per cycle. DRAM latency: ~50-100 ns = 150-400 CPU cycles wasted per cache miss. This gap has widened over decades as CPU speeds scaled faster than DRAM speeds, making the memory hierarchy critical for performance.",
    "ift212_028": "Speed/size trade-off: Registers (< 1 cycle, ~KB) → L1 Cache (1-4 cycles, ~32-64KB) → L2 Cache (10-20 cycles, ~256KB-1MB) → L3 Cache (30-50 cycles, ~4-32MB) → DRAM (100-300 cycles, ~GBs) → SSD (thousands of cycles, ~TBs).",
    "ift212_029": "Temporal locality example: a loop counter variable accessed on every iteration. Spatial locality example: iterating through an array — fetching element[0] brings elements[1]-[15] into the cache line automatically. Both principles make caches effective in practice.",
    "ift212_030": "Write-back vs Write-through: Write-through instantly writes to both cache and RAM (simpler, slower). Write-back only writes to cache, sets dirty bit=1, and defers RAM update until eviction (faster, but requires dirty bit tracking and more complex cache coherence protocols).",
    "ift212_031": "Harvard architecture L1 split: I-cache feeds the Fetch stage while D-cache serves the Memory stage simultaneously. Without the split, a unified cache would create a structural hazard — the Fetch and Memory stages would compete for the same cache port every cycle.",
    "ift212_032": "AMAT = Hit Time + (Miss Rate × Miss Penalty). Example: Hit Time = 1 cycle, Miss Rate = 5%, Miss Penalty = 200 cycles → AMAT = 1 + (0.05 × 200) = 1 + 10 = 11 cycles. Reducing any of the three terms improves AMAT.",
    "ift212_033": "Array traversal: int arr[1000] is stored in 1000 consecutive memory locations. Fetching arr[0] loads a 64-byte cache line containing arr[0] through arr[15] simultaneously. Linked list nodes each require a pointer dereference to a random address, causing a cache miss on nearly every node access.",
    "ift212_034": "Without virtual memory: all programs share one flat physical address space. Any pointer bug, buffer overflow, or malicious write can corrupt any other program's memory or the OS kernel. Virtual memory gives each process a private address space enforced by hardware (MMU).",
    "ift212_035": "Segmentation translation: Physical Address = Segment_Base + Offset. The Segment Descriptor Table holds (Base, Limit, Protection bits) per segment. If Offset > Limit → hardware raises a Segmentation Fault. Segments vary in size (code segment, data segment, stack segment).",
    "ift212_036": "External fragmentation scenario: Process A (200KB) loads, Process B (400KB) loads, Process A exits leaving a 200KB hole. Process C needs 300KB — the 200KB hole is too small even though total free memory may be sufficient. Compaction (moving segments) is slow and expensive.",
    "ift212_037": "Paging: virtual address space divided into fixed pages (e.g., 4KB each); physical RAM divided into frames of the same size. Any free frame can hold any page — no size mismatch possible. A Page Table maps virtual page numbers to physical frame numbers.",
    "ift212_038": "Internal fragmentation example: page size = 4KB, process needs 5KB → allocated 2 pages = 8KB. Wasted space = 8KB - 5KB = 3KB locked inside the second frame. Smaller pages reduce internal fragmentation but increase page table size and TLB pressure.",
    "ift212_039": "Page table entry bits: Valid/Present bit (1=in RAM, 0=on disk), Dirty bit (write-back), Reference/Accessed bit (for replacement algorithms), Protection bits (read/write/execute permissions), and the Physical Frame Number (PFN). A valid=0 access triggers a page fault interrupt.",
    "ift212_040": "TLB operation: on every memory access, the MMU first checks the TLB for a cached virtual→physical translation. TLB hit (common case): translation in ~1 cycle. TLB miss: walk the page table in RAM (~100+ cycles), cache the result in TLB. Typical TLB hit rate: >99%, making virtual memory overhead nearly negligible.",
    "ift212_041": "Von Neumann architecture uses a single bus connecting CPU and memory. This means instruction fetch and data read/write operations are mutually exclusive — they cannot happen simultaneously. The CPU must wait for one transfer to complete before starting the next, creating a fundamental throughput ceiling.",
    "ift212_042": "MAR is connected directly to the address bus. When the CPU needs to read or write memory, it places the target address into the MAR. The memory controller reads the MAR value and activates the corresponding physical address lines to select the correct memory cell.",
    "ift212_043": "MAR = where (address). MBR/MDR = what (data). READ: memory places fetched data into MBR → CPU reads from MBR. WRITE: CPU places data into MBR → MBR drives the data bus → data is written to the address in MAR. They work as a pair on every memory transaction.",
    "ift212_044": "Fetch phase: PC → MAR → address bus activates memory → instruction bits arrive in MBR → MBR → IR. Simultaneously, PC = PC + instruction_size (e.g., PC+4 for 32-bit RISC). The PC now points to the next instruction, ready for the next fetch cycle.",
    "ift212_045": "The IR holds the full binary instruction word during decode and execute phases. The control unit reads the opcode field from the IR to determine what operation is needed, then reads the operand fields to determine sources and destinations.",
    "ift212_046": "Immediate mode: the constant is encoded inside the instruction itself, e.g., MOV EAX, 45 → the value 45 is in the instruction word. No extra memory access needed. Limitation: the constant size is limited by the instruction word's immediate field width (e.g., 8-bit, 16-bit).",
    "ift212_047": "Register Direct: operand IS the register value — ADD R1, R2 adds R2's value to R1. Register Indirect: register holds a POINTER — LOAD R1, (R2) reads from the memory address stored in R2. Indirect requires one extra memory access cycle to dereference the pointer.",
    "ift212_048": "Indexed EA = Base_Register + Displacement. Example: LOAD R1, 8(R2) → EA = R2 + 8. If R2 = 1000, EA = 1008. This is the fundamental mechanism behind array indexing: R2 points to array base, displacement selects the element offset within the array.",
    "ift212_049": "Direct mapping formula: cache_line = block_address mod num_cache_lines. Simple, fast, cheap — requires only index bits to locate the line. Problem: thrashing — if two frequently-used blocks map to the same line, they evict each other repeatedly despite other lines being empty.",
    "ift212_050": "Fully associative: a block can go into ANY cache line. Lookup requires comparing the tag against ALL cache lines in parallel (requires CAM — content-addressable memory). Zero conflict misses. Cost: expensive hardware, high power consumption. Used in TLBs and small critical caches.",
    "ift212_051": "N-way set-associative: cache is divided into S sets, each holding N lines. A block maps to exactly one set (like direct mapping) but can occupy any of the N lines within that set (like fully associative). 2-way and 4-way are the most common practical configurations.",
    "ift212_052": "Address bit breakdown for direct-mapped cache: [Tag | Index | Offset]. Offset bits = log2(block_size_in_bytes). Index bits = log2(number_of_cache_lines). Tag bits = remaining address bits. On access: use Index to find line, compare Tag to check hit, use Offset to extract the byte.",
    "ift212_053": "Three C's of cache misses: Compulsory (cold) — first access to any block, unavoidable; Capacity — cache too small to hold working set; Conflict — multiple blocks compete for same set/line. Larger caches reduce capacity misses; higher associativity reduces conflict misses.",
    "ift212_054": "Write-through: every store instruction writes to cache AND main memory simultaneously. Pros: simple, always coherent. Cons: every write stalls on slow DRAM latency. Write buffers partially mitigate this. Write-back is far more common in modern CPUs for performance reasons.",
    "ift212_055": "Page fault sequence: (1) MMU detects valid=0 in page table → (2) raises hardware interrupt → (3) OS page fault handler runs → (4) OS finds a free frame (may evict dirty page) → (5) OS reads missing page from disk into frame → (6) updates page table entry, sets valid=1 → (7) restarts faulting instruction.",
    "ift212_056": "LRU approximates the optimal replacement algorithm (OPT). It uses access timestamps or reference bits to identify the least recently used page. In practice, hardware tracks reference bits that are periodically cleared; the page with the oldest reference is chosen for eviction.",
    "ift212_057": "Thrashing cause: each process needs more frames than available → every page fault brings in a page that immediately evicts another page that will be needed again → CPU utilization collapses to near 0% as all time is spent on disk I/O. Solution: reduce multiprogramming degree or increase RAM.",
    "ift212_058": "Traditional page table size = (virtual_pages) × (entry_size). For a 64-bit address space with 4KB pages: 2^52 entries — astronomically large. Inverted page table size = physical_frames × entry_size — fixed and small regardless of virtual address space size. Lookup requires searching by (PID, virtual_page).",
    "ift212_059": "Dirty bit = 0: page not modified since loaded → on eviction, just overwrite the frame (data on disk is still valid). Dirty bit = 1: page was written to → on eviction, must write the page back to disk before reusing the frame. This avoids unnecessary disk writes for read-only pages.",
    "ift212_060": "MMU sits between the CPU pipeline and the L1 cache on modern processors. Every memory address generated by executing code passes through the MMU. The MMU checks the TLB first, then walks the page table on a miss, and raises a page fault interrupt if the page is not in RAM.",
    "ift212_061": "Assembly is symbolic machine code. Each mnemonic (MOV, ADD, JMP) maps 1:1 to a specific binary opcode defined by the CPU's ISA. This gives programmers precise hardware control — a privilege unavailable in high-level languages where one statement expands into many machine instructions.",
    "ift212_062": "The assembler reads .asm source text, resolves labels to addresses, converts mnemonics to binary opcodes, and outputs an object file (.o). A linker then combines object files into a final executable binary. No runtime interpretation occurs — the output is native machine code.",
    "ift212_063": "x86 registers (EAX, EBX, ESP) and ARM registers (R0-R15) have different names, sizes, and instructions. An x86 ADD EAX, EBX encodes to a specific bit pattern that ARM hardware cannot decode. Portability requires rewriting the entire program for the target ISA.",
    "ift212_064": ".data section is mapped to a read-write data segment in the process memory layout. It holds pre-initialised values that exist before main() runs. Examples: msg db 'Hello', 0 (string constant), count dd 0 (initialised integer). Compare with .bss which holds zero-initialised/uninitialised variables.",
    "ift212_065": ".text (also called .code) is marked read-only and executable by the OS. The CPU's PC register points into this section to fetch instructions. Attempting to write to .text raises a segmentation fault — this separation protects against self-modifying code attacks.",
    "ift212_066": "Labels are symbolic names for memory addresses. During assembly, the assembler builds a symbol table mapping each label to its byte offset. A branch instruction like JMP loop_entry is then encoded with the resolved numeric address or a relative displacement value.",
    "ift212_067": "Directives (pseudo-ops): db, dw, dd (allocate data), equ (define constant), section (declare segment), global (export symbol), extern (import symbol). None of these generate CPU opcodes — they guide the assembler's code generation process only. Instructions like MOV, ADD, PUSH produce actual binary opcodes.",
    "ift212_068": "if (A > B) → assembly: CMP A, B → JLE else_label → [then-block] → JMP end_label → else_label: [else-block] → end_label. CMP performs A-B and sets flags. JLE (Jump if Less or Equal) branches to else if condition fails; the then-block runs if condition passes.",
    "ift212_069": "After the then-block executes, the PC would naturally advance to the next sequential byte — which is the first instruction of the else-block. The JMP end_label skips over the else-block by setting PC = end_label address, ensuring mutual exclusivity of the two branches.",
    "ift212_070": "while (i < 10) { body } → assembly: loop_top: CMP i, 10 → JGE loop_end → [body] → JMP loop_top → loop_end. The backward JMP loop_top re-evaluates the condition. JGE exits when i >= 10. A do-while loop places the test at the bottom instead.",
    "ift212_071": "CALL mechanics: SP = SP - 4 (or 8 on 64-bit), [SP] = PC+instruction_size (return address saved to stack), PC = target_address. This ensures that after the subroutine finishes, RET can recover the exact address to resume execution in the caller.",
    "ift212_072": "RET mechanics: PC = [SP] (load saved return address), SP = SP + 4. This is the exact inverse of CALL. The stack is LIFO — the most recently pushed return address is always on top, ensuring nested calls unwind correctly in reverse order.",
    "ift212_073": "On x86: RSP (64-bit) / ESP (32-bit). Stack grows downward — PUSH decrements SP then stores, POP loads then increments SP. SP always points to the valid top-of-stack item. Never use SP as a general-purpose register while the stack is active.",
    "ift212_074": "EBP (x86) / FP (generic): set to ESP at procedure entry (FP = SP). All local variables are at negative offsets (e.g., [EBP-4], [EBP-8]) and parameters at positive offsets ([EBP+8], [EBP+12]). Even as ESP moves during the function body, EBP stays fixed as the frame anchor.",
    "ift212_075": "Stack frame layout (high to low address): [caller's parameters] → [return address] → [saved FP] → [saved callee registers] → [local variables]. The prologue builds this structure; the epilogue tears it down by restoring FP and SP before executing RET.",
    "ift212_076": "A calls B: A's frame is pushed. B calls C: B's frame is pushed on top. C returns: C's frame is popped, B resumes. B returns: B's frame is popped, A resumes. LIFO guarantees perfect reversal regardless of call depth. Stack overflow occurs only if nesting depth exceeds available stack memory.",
    "ift212_077": "factorial(3) calls factorial(2) calls factorial(1) calls factorial(0). Each call gets its own frame with its own n parameter and return address. factorial(0) returns 1 → frame popped. factorial(1) returns 1 → frame popped. Each return unwinds one level until factorial(3) completes.",
    "ift212_078": "x86 calling convention: Caller-saved (volatile): EAX, ECX, EDX — subroutine may destroy these freely; caller backs them up if needed. Callee-saved (non-volatile): EBX, ESI, EDI, EBP — subroutine must push these at entry and pop them before RET. This contract enables reliable interoperability between functions.",
    "ift212_079": "Autoincrement sequence: (1) EA = Reg, (2) load operand from memory[EA], (3) Reg = Reg + size. The register is used THEN updated. Perfect for forward array traversal: Reg starts at array[0], after each LOAD it advances to array[1], array[2], etc., without explicit index arithmetic.",
    "ift212_080": "Autodecrement sequence: (1) Reg = Reg - size, (2) EA = Reg, (3) load operand from memory[EA]. The register is updated THEN used. Together: autoincrement = stack pop (post-increment), autodecrement = stack push (pre-decrement). These two modes directly implement hardware stack push/pop operations.",

    "ift212_081": "Control signals are the actual low-level hardware commands that drive the CPU data path. They dictate which ALU operations occur, open/close registers for data transfers, enable memory reads/writes, and configure multiplexer routing — every coordinated action inside the CPU is governed by these electrical lines.",
    "ift212_082": "The Control Unit must know (1) what instruction is executing — IR opcode, (2) what status conditions exist — Condition Flags (Zero, Carry, Negative, Overflow) for correct branching decisions, and (3) when to advance to the next state — System Clock ticks drive the CU's finite state machine.",
    "ift212_083": "Hardwired control structures use fixed combinational logic gates to map opcodes directly to control wires. This produces the fastest possible execution with minimal latency since there is no ROM lookup delay. The trade-off is rigidity — any change to instruction behavior requires a physical chip redesign. RISC architectures (ARM, MIPS, RISC-V) use this approach.",
    "ift212_084": "Microprogrammed control abstracts hardware signals into software-like microcode. An instruction opcode acts as an entry-point address into an internal ROM (Control Store). The CU steps through micro-instructions in this ROM to emit the required control patterns each clock cycle. This is typical of complex CISC architectures like x86.",
    "ift212_085": "Hardwired logic runs at maximum speed because it avoids memory lookup cycles — but altering it requires a physical chip redesign. Microprogrammed logic introduces an internal memory-access delay (making it slower), but its microcode can be easily rewritten or extended to handle complex or patched instructions.",
    "ift212_086": "Modern x86 processors (Intel/AMD) combine both philosophies: a hardware decoder translates x86 instructions into RISC-like micro-ops. Simple, frequent instructions (ADD, LOAD, MOV) are handled by hardwired fast paths for maximum throughput. Rare or complex instructions — and security patches like Spectre mitigations — invoke microcode from an internal patchable control store.",
    "ift212_087": "Horizontal microprogramming: one bit per control signal — the control store word is very wide (100+ bits) but no decoder is needed; all signals are asserted in parallel. Vertical microprogramming: mutually exclusive signals are encoded into narrow fields (e.g., 3 bits representing 8 ALU operations), dramatically reducing ROM width at the cost of a small decoder delay at the destination.",
    "ift212_088": "AMAT = Hit Time + (Miss Rate × Miss Penalty). Example: Hit Time = 1 cycle, Miss Rate = 5%, Miss Penalty = 200 cycles → AMAT = 1 + (0.05 × 200) = 11 cycles. Reducing any of the three terms improves AMAT. Larger caches reduce Miss Rate; better cache design reduces Hit Time; prefetching reduces effective Miss Penalty.",
    "ift212_089": "Multi-level AMAT cascades: if the CPU misses L1, it searches L2. If it misses L2, it goes to main memory. Formula: AMAT = L1_HitTime + (L1_MissRate × [L2_HitTime + (L2_MissRate × L2_MissPenalty)]). Each level's miss penalty includes the cost of checking the next level down.",
    "ift212_090": "Temporal locality: a loop counter variable is read/written on every iteration — the same address is accessed repeatedly in a short time window. Spatial locality: iterating through int arr[] reads arr[0], arr[1], arr[2] sequentially — adjacent memory addresses are accessed close in time. Caches exploit both properties to hide DRAM latency.",
    "ift212_091": "Cache hardware fetches an entire 64-byte cache line on a miss. Loading arr[0] automatically pre-loads arr[1]–arr[15] into the same line — perfect spatial locality. Linked list nodes are heap-allocated at arbitrary addresses; following a next pointer typically requires fetching a completely different cache line, causing a miss on nearly every node traversal.",
    "ift212_092": "Programmed I/O (busy-wait/polling): the CPU executes a tight loop continuously checking the device's status register bit. While waiting for a slow peripheral (keyboard, disk, network — milliseconds to seconds), the processor burns billions of clock cycles doing nothing useful. This wastes CPU throughput that could serve other processes.",
    "ift212_093": "Interrupt-driven I/O: CPU issues the I/O command, then resumes normal execution on other tasks. When the device finishes, it asserts an interrupt request line (IRQ). The CPU completes its current instruction, saves context (PC, registers) onto the stack, jumps to the Interrupt Service Routine (ISR) to handle the data, then returns to the interrupted program seamlessly.",
    "ift212_094": "DMA controller: the CPU programs it with source/destination addresses and byte count, then signals it to start. The DMA takes control of the address and data buses (CPU is bus-paused via cycle-stealing or burst mode) and transfers the entire block directly between the peripheral and RAM. Only one interrupt is sent to the CPU when the full transfer is complete — drastically reducing CPU overhead for bulk I/O.",
    "ift212_095": "Before releasing the bus to the DMA controller, the CPU loads: (1) Starting Memory Address — where in RAM to write/read the data block, and (2) Byte/Word Counter — how many units to transfer before asserting the completion interrupt. Without these two values, the DMA has no idea where to put data or when to stop.",
    "ift212_096": "In a multi-core CPU, Core A and Core B each have private L1/L2 caches. If Core A writes variable X = 99, Core B may still cache X = 0 from before. MESI protocol assigns each cache line a state: Modified, Exclusive, Shared, or Invalid. When Core A modifies X, the protocol broadcasts an invalidation signal, forcing Core B to discard its stale copy and re-fetch the updated value.",
    "ift212_097": "Only one device can drive signals onto a shared bus at a time. When both the CPU and a DMA controller request the bus simultaneously, the Bus Arbiter resolves the conflict. Common schemes: daisy-chain (priority by physical position), centralized polling (arbiter polls masters in turn), and distributed arbitration (masters negotiate via dedicated request/grant lines).",
    "ift212_098": "CISC (e.g., x86): hundreds of complex, variable-length instructions (1–15 bytes). A single instruction like MOVSB copies a memory block — doing what takes several RISC instructions. Goal: minimize code size, ease assembly programming. Cost: complex, slow decoder. RISC (e.g., ARM, RISC-V): ~50–100 fixed-length 32-bit instructions. Load/store architecture — arithmetic only on registers. Goal: simple decoder, single-cycle execution, ideal for pipelining.",
    "ift212_099": "A single CISC macro-instruction like ADD [EAX], EBX decomposes into micro-ops: (1) read memory at address in EAX, (2) route value to ALU input, (3) add EBX, (4) write result back to memory. Modern x86 CPUs decode instructions into these atomic micro-ops internally, allowing the out-of-order execution engine to schedule and execute them independently for maximum throughput.",
    "ift212_100": "Pipeline fetch-ahead problem: when a conditional branch (BNE, BEQ, JZ) enters the pipeline, the processor has already fetched the next 2–4 sequential instructions. If the branch is taken, those pre-fetched instructions are wrong and must be flushed (squashed), wasting cycles. Solutions: branch prediction (guess taken/not-taken), delayed branching (execute instructions after branch regardless), and branch target buffers (cache branch outcomes)."
};

if (typeof module !== 'undefined' && module.exports) {
    module.exports = IFT212_EXPLANATIONS;
}

